# useColorScheme

get browser color by system setting's color (`prefers-color-scheme`)

if `auto`, it returns `light` as defaut

## Usage

<CodeBlock>useColorScheme();</CodeBlock>

## Live Demo

Change system color or browser color to see button color changes

import Demo from './demo';

<Demo />

## Code

import CodeBlock from '@/components/CodeBlock';
import demo from '!!raw-loader!./demo';

<CodeBlock>{demo}</CodeBlock>
