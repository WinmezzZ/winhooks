import CodeBlock from '@/components/CodeBlock';
import demo from '!!raw-loader!./demo';
import iifeDemo from '!!raw-loader!./iife.demo';

# useAsyncEffect

该 hooks 可以在 ts 的 `useEffect` hooks 中使用 `await` 语法

> 这只是一个类似语法糖，没有修改任何逻辑，利用了类型重写

## 示例代码

<CodeBlock language="language-tsx">{demo}</CodeBlock>

#### 你同样可以使用 `IIFE` 立即执行函数来跳过 ts 检查

<CodeBlock language="language-tsx">{iifeDemo}</CodeBlock>;
